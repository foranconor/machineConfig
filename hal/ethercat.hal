# Both addf calls are here because servo-thread is created by core_sim.hal.
# read-all runs after motion-controller for now — acceptable for bench use.
addf lcec.read-all  servo-thread
addf lcec.write-all servo-thread

# Pin connections are stubbed out until we confirm actual pin names.
# Start LinuxCNC, then run:  halcmd show pin lcec
# and paste the output to get the real names.


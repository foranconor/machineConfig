# hal

Custom HAL files for myCoolMachine.

Currently empty — sim HAL files (core_sim, sim_spindle_encoder, simulated_home, axis_manualtoolchange) are
pulled from ~/linuxcnc/configs/sim.axis by deploy.sh. They belong here once we own them.
